{% extends 'base.html.twig' %}

{% block title %}Add New Product - The Pet Pantry{% endblock %}

{% block body %}
<div class="form-wrapper">
    <div class="form-card">
        <h1>🐾 Add New Product</h1>

        <div class="form-body">
            {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}

            <div class="form-group">
                {{ form_label(form.name) }}
                {{ form_widget(form.name) }}
                {{ form_errors(form.name) }}
            </div>

            <div class="form-group">
                {{ form_label(form.price) }}
                {{ form_widget(form.price) }}
                {{ form_errors(form.price) }}
            </div>

            <div class="form-group">
                {{ form_label(form.description) }}
                {{ form_widget(form.description) }}
                {{ form_errors(form.description) }}
            </div>

            <!-- ✅ Fixed Image Upload Section -->
            <div class="form-group">
                <label for="product_image" class="custom-file-label">📸 Upload Product Image</label>
                {{ form_widget(form.image, {'attr': {'class': 'custom-file-input'}}) }}
                {{ form_errors(form.image) }}
            </div>

            <!-- ✅ Category Field -->
            <div class="form-group">
                {{ form_label(form.category) }}
                {{ form_widget(form.category) }}
                {{ form_errors(form.category) }}
            </div>

            <div class="form-actions">
                <button class="btn submit">💾 Save Product</button>
            </div>

            {{ form_end(form) }}
        </div>

        <div class="form-actions">
            <a href="{{ path('app_product_index') }}" class="btn back">← Back to List</a>
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    .form-wrapper {
        display: flex;
        justify-content: center;
        align-items: flex-start;
        padding: 40px 20px;
    }

    .form-card {
        background: rgba(255, 250, 240, 0.95);
        backdrop-filter: blur(8px);
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(139, 94, 60, 0.15);
        max-width: 850px;
        width: 100%;
        padding: 50px 60px;
        animation: fadeIn 0.6s ease-in-out;
    }

    .form-card h1 {
        text-align: center;
        font-size: 2rem;
        color: #5C4033;
        margin-bottom: 30px;
        font-weight: 700;
    }

    .form-group {
        margin-bottom: 18px;
    }

    label {
        font-weight: 600;
        color: #4E342E;
        margin-bottom: 5px;
        display: block;
    }

    input[type="text"],
    input[type="number"],
    textarea,
    select {
        width: 100%;
        padding: 12px 15px;
        border: 2px solid #E0B184;
        border-radius: 12px;
        background-color: #FFF8E1;
        font-family: 'Poppins', sans-serif;
        transition: all 0.3s ease;
    }

    /* ✅ Custom File Upload Styling */
    .custom-file-input {
        width: 100%;
        padding: 12px 15px;
        border: 2px dashed #E0B184;
        border-radius: 12px;
        background-color: #FFF8E1;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .custom-file-input:hover {
        border-color: #FB8C00;
        background-color: #FFF3E0;
    }

    .custom-file-label {
        font-weight: 600;
        color: #4E342E;
        display: block;
        margin-bottom: 8px;
    }

    input:focus,
    textarea:focus,
    select:focus {
        border-color: #FB8C00;
        background-color: #FFF3E0;
        box-shadow: 0 0 8px rgba(251, 140, 0, 0.25);
        outline: none;
    }

    select {
        appearance: none;
        background-image: url("data:image/svg+xml;charset=UTF-8,%3Csvg width='10' height='6' viewBox='0 0 10 6' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath fill='%23666666' d='M0 0l5 6 5-6z'/%3E%3C/svg%3E");
        background-repeat: no-repeat;
        background-position: right 12px center;
        background-size: 10px;
    }

    button {
        background-color: #FB8C00;
        color: white !important;
        font-weight: 600;
        font-size: 1rem;
        border: none;
        border-radius: 25px;
        padding: 12px 30px;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-top: 10px;
        box-shadow: 0 4px 10px rgba(251, 140, 0, 0.25);
    }

    button:hover {
        background-color: #F57C00;
        transform: translateY(-2px);
        box-shadow: 0 6px 15px rgba(251, 140, 0, 0.35);
    }

    .form-actions {
        display: flex;
        justify-content: center;
        margin-top: 35px;
    }

    .btn.back {
        background-color: #FFE0B2;
        color: #4E342E;
        font-weight: 600;
        padding: 12px 28px;
        border-radius: 25px;
        text-decoration: none;
        border: 2px solid transparent;
        transition: all 0.3s ease;
    }

    .btn.back:hover {
        background-color: #FFB74D;
        color: #fff;
        transform: translateY(-2px);
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 768px) {
        .form-card {
            padding: 30px 25px;
        }

        button {
            width: 100%;
        }

        .btn.back {
            width: 100%;
            text-align: center;
        }
    }
</style>
{% endblock %}
