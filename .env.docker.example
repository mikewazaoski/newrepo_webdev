# Optional: copy to .env.docker and run: docker compose --env-file .env.docker up -d

APP_PORT=8080
MYSQL_PORT=3307
PHPMYADMIN_PORT=8081
APP_SECRET=pet_pantry_docker_change_this_secret_32chars
DEFAULT_URI=http://localhost:8080
